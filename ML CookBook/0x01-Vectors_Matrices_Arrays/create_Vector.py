# Using Numpy to create a one-dimensional array:

import numpy as np

# create vector as a row.
vector_row = np.array([1, 2, 3])

# create vector as a column.
vector_column = np.array([
    [1],
    [2],
    [3]
])

# A vector is just an array with a single dimension.