import numpy as np

# Generate a vector of shape (1, 5) containing all zeros.
vector = np.zeros(shape=5)

print(vector)

# Generating a matrix of shape (3, 3) containing all ones.
matrix = np.full(shape=(3,3), fill_value=1)

print(matrix)